<section class="page-content">
  <div class="body-container">
    <h1 class="page-title">Adoptions</h1>
    <% for adoption in @adoptions %>
      <img class="animal-pictures" src="<%=adoption.animal.picture%>"/>
      <p>Name: <%= adoption.owner.first_name %></p>
      <p>Adopted Pet: <%= adoption.animal.name %></p>
    <hr/>
    <% end %>
  </div>
</section>